function bingo(bing) {
  let count=0;
  for (let i=0;i<a.length;i++)
  {
  if (bing[i]===2){ count++}
  if (bing[i]===7){ count++}
  if (bing[i]===9){ count++}
  if (bing[i]===14){ count++}
  if ([bingi]===15){ count++}
  }
  return count>=5?"WIN":"LOSE";
}